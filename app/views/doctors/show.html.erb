<h1>Welcome to Dr. <%= @doc.name %>'s Appointments Page</h1>

<h2>Appointments:<br><br>
<ul>
<% @doc.appointments.each do |apt| %>

<li>Dr. <%= @doc.name %> has an appointment with
<%= link_to apt.ptnt_name, patient_path(apt.patient) %> on
<%= apt.time %>.</li>

<% end %>
</h2>
