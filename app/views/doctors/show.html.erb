Name: <%= @doctor.name %><br>
Department: <%= @doctor.department %><br>
Appointments: <% @doctor.appointments.each do |appt| %>
<%= appt.appointment_datetime.strftime('%B %d, %Y') %> at <%=appt.appointment_datetime.strftime('%k:%M')%> with <%= link_to appt.patient.name, patient_path(appt.patient)%> <br>
<% end %>