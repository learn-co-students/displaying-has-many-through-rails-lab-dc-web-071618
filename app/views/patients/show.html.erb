<h2><%= @patient.name %></h2>
<h3>Appointments</h3>
  <ul style="list-style-type:square">
    <% @patient.appointments.each do |a| %>
      <li>
        <%= a.formatted_datetime %> with
        <%= link_to a.doctor.name, doctor_path(a.doctor) %>
      </li>
    <% end %>
  </ul>
